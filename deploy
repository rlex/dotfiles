#!/bin/bash
dotfiles=( screenrc vimrc vim zshrc mc bashrc bash_profile gemrc htoprc gitconfig hgrc biewrc dircolors)
for file in ${dotfiles[@]}
do
        ln -sfv ~/.rc/.$(basename $file) ~/.$file
done
cd - >&/dev/null
